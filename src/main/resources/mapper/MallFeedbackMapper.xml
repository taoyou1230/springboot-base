<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.springboot.base.module.dao.MallFeedbackMapper">
	<resultMap id="BaseResultMap" type="com.springboot.base.module.entity.MallFeedback">
		<id column="id" property="id" jdbcType="INTEGER"/>
		<result column="mall_mp_user_id" property="mallMpUserId" jdbcType="INTEGER"/>
		<result column="content" property="content" jdbcType="VARCHAR"/>
		<result column="reply" property="reply" jdbcType="VARCHAR"/>
		<result column="created_at" property="createdAt" jdbcType="TIMESTAMP"/>
		<result column="updated_at" property="updatedAt" jdbcType="TIMESTAMP"/>
	</resultMap>

	<sql id="Base_Column_List">
		id,mall_mp_user_id,content,reply,created_at,updated_at
	</sql>

	<sql id="Sort_Condition">
		<trim suffixOverrides=",">
		order by
		<choose>
			<when test="sortInfo != null and sortInfo.size>0">
				<if test="sortInfo.id =='desc'">
					id desc,
				</if>
				<if test="sortInfo.id =='asc'">
					id asc,
				</if>
				<if test="sortInfo.mallMpUserId =='desc'">
					mall_mp_user_id desc,
				</if>
				<if test="sortInfo.mallMpUserId =='asc'">
					mall_mp_user_id asc,
				</if>
				<if test="sortInfo.content =='desc'">
					content desc,
				</if>
				<if test="sortInfo.content =='asc'">
					content asc,
				</if>
				<if test="sortInfo.reply =='desc'">
					reply desc,
				</if>
				<if test="sortInfo.reply =='asc'">
					reply asc,
				</if>
				<if test="sortInfo.createdAt =='desc'">
					created_at desc,
				</if>
				<if test="sortInfo.createdAt =='asc'">
					created_at asc,
				</if>
				<if test="sortInfo.updatedAt =='desc'">
					updated_at desc,
				</if>
				<if test="sortInfo.updatedAt =='asc'">
					updated_at asc,
				</if>
			</when>
			<otherwise>
				created_at desc
			</otherwise>
		</choose>
		</trim>
	</sql>

	<sql id="Query_Condition">
		1=1
		<if test="filters !=null and filters.size>0">
			<if test="filters.id !=null and filters.id !=0">
				and id = #{filters.id}
			</if>
			<if test="filters.mallMpUserId !=null and filters.mallMpUserId !=0">
				and mall_mp_user_id = #{filters.mallMpUserId}
			</if>
			<if test="filters.content !=null and filters.content !=''">
				and content = #{filters.content}
			</if>
			<if test="filters.reply !=null and filters.reply !=''">
				and reply = #{filters.reply}
			</if>
			<if test="filters.createdAt !=null">
				and created_at = #{filters.createdAt}
			</if>
			<if test="filters.updatedAt !=null">
				and updated_at = #{filters.updatedAt}
			</if>
		</if>
	</sql>

	<select id="queryByCondition" resultMap="BaseResultMap" parameterType="java.util.Map">
		<where>
			<include refid="Query_Condition"/>
		</where>
		<include refid="Sort_Condition"/>
	</select>
</mapper>